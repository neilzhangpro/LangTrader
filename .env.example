# =============================================================================
# LangTrader Agents 环境配置模板
# 
# 使用方法: 
#   1. 复制此文件为 .env: cp .env.example .env
#   2. 修改 .env 中的数据库配置
#   3. 启动服务
#
# 极简配置: 
#   - 只需配置数据库连接！
#   - 交易所 API Key、LLM API Key、LangSmith Key 等所有配置
#     都通过前端界面配置并存储在数据库中
# =============================================================================

# -----------------------------------------------------------------------------
# Docker 部署时使用以下配置
# -----------------------------------------------------------------------------
POSTGRES_USER=langtrader
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=langtrader_pro

# -----------------------------------------------------------------------------
# 本地开发时使用以下配置
# （Docker 部署时此项由 docker-compose 自动生成，无需手动配置）
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://username:password@localhost:5432/langtrader_pro
API_KEYS='["dev-key-123"]'
